[project]
name = "sdr-m3u-tuner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.2.6",
]

[tool.ruff]
extend-exclude = [
	"gnuradio/"
]
