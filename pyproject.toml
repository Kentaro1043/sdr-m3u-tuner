[project]
name = "sdr-m3u-tuner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask==3.1.1",
    "loguru==0.7.3",
    "numpy==2.2.6",
    "pyyaml==6.0.2",
]

[tool.ruff]
extend-exclude = [
    "**/gr/**"
]
